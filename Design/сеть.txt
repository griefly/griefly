


Сервер -> клиенты

Клиент может управлять одним существом.

Клиент сделал что-то над существом -> перезагрузим это существо


шлем для каждого существа его действия 
макрос типа int SEND_FUNK(name, агрумент)
	     {
		
             }
разворачивается в
             funkAdder(); 
             int name(aргумент)
	     {
		send(hash("name"), аргумент)
             }