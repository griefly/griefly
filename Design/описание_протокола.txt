1. Сообщение-логин от клиента:

number - НЕ ИСПОЛЬЗУЕТСЯ
from   - игровой id клиента
to     - специальный токен для данного jid, для проверки 
type   - НЕ ИСПОЛЬЗУЕТСЯ
text   - jid клиента (например, lagius@jabber.ru)

2. Сообщение-логин клиенту:

number - НЕ ИСПОЛЬЗУЕТСЯ
from   - игровой id клиента
to     - НЕ ИСПОЛЬЗУЕТСЯ 
type   - НЕ ИСПОЛЬЗУЕТСЯ
text   - "map"/"nomap" - будет ли следовать за этим карта.

3. Запрос создания аватары клиента доверенному клиенту:

number - НЕ ИСПОЛЬЗУЕТСЯ
from   - уникальный id клиента, для кого создается 
to     - НЕ ИСПОЛЬЗУЕТСЯ
type   - системный тип
text   - "give_creator"

4. Ответ создания аватары от доверенного клиента:

number - НЕ ИСПОЛЬЗУЕТСЯ
from   - уникальный id клиента, для кого создается 
to     - игровой id клиента
type   - системный тип
text   - "create"

5. Запрос карты у доверенного клиента

number - НЕ ИСПОЛЬЗУЕТСЯ
from   - НЕ ИСПОЛЬЗУЕТСЯ
to     - уникальный id клиента, для кого создается 
type   - системный тип
text   - "need_map"

6. Ответ карты от доверенного клиента

number - НЕ ИСПОЛЬЗУЕТСЯ
from   - номер последнего обработанного сообщения
to     - уникальный id клиента, для кого создается 
type   - тип карты
text   - сама карта

7. Структура заголовка карты

[количество тактов]
[количество обьектов (id)]
[id управляемого обьекта]
[сид генератора рч]
[количества вызовов генератора рч]
[создатель всего]

8. Структура предмета в карте

(Корневой объект)

[тип предмета (хэш)]
[номер предмета]
[номер мастера предмета (owner)]

(Следущий по иеархии)

[переменная1]
[переменная2]

...