1)Из равок загружаются рецепты

2)Из равок подгружается существо в формате установки переменных
           переменная = значение, с помощью функций сэйв/лоад

3)Отдельно загружается система органов.
           то есть в конструкторе(с параметром bool для отсутствия лишней инициализации)
           прописывается id_ptr = newItem(hash("OrganSystem")) и после loadOrganSytem

4)Возможность создания своих существ. Главная суть
      MUTABLE FROM [MONSTR] TO [MYMONSTR] #это захэшируется
      
      [тут своя строка иницализации]

Реализации - map функций теперь хранит структуру указатель на функцию создания + строка
инициализации - возможно плохо.
При этом храним указатель созданей от нашего MUTABLE. Проблема - гет_тайп будет тот же.

5)Генерация карты. 
      Желательно скриптовой. Генерация а-ля

      ЛЛЛЛЛЛЛЛЛЛЛЛЛ
      ЛОООООЛЛЛЛЛЛЛ
      ЛЛООООллллAAA
      ЛЛЛОЛЛЛЛЛЛААА
      ЛЛЛЛЛЛЛЛЛЛЛЛЛ  где о озеро, л лес, а - асфальт
      с возможностью в любой момент времени догенерировать в любую сторону квадрат

      и генерация собственно игрового поля.

6)Крафт?